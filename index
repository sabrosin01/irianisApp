cat > /home/claude/irianis-pwa/index.html << 'HTMLEOF'
<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0"/>
<meta name="apple-mobile-web-app-capable" content="yes"/>
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent"/>
<meta name="apple-mobile-web-app-title" content="Irianis"/>
<link rel="manifest" href="manifest.json"/>
<link rel="apple-touch-icon" href="icons/icon-192.png"/>
<meta name="theme-color" content="#0A0A0A"/>
<title>Irianis Marcano – Estilista & Nails</title>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,600;1,400&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet"/>
<style>
:root{--gold:#C9A84C;--gold-light:#E8C96A;--gold-dim:#8B6914;--black:#0A0A0A;--surface:#111111;--surface2:#1A1A1A;--surface3:#222222;--border:#2A2A2A;--text:#F0EDE6;--text-muted:#888;--radius:14px;--safe-bottom:env(safe-area-inset-bottom,16px);}
*{box-sizing:border-box;margin:0;padding:0;-webkit-tap-highlight-color:transparent;}
html,body{height:100%;background:var(--black);color:var(--text);font-family:'Inter',sans-serif;overflow:hidden;}

/* SPLASH */
#splash{position:fixed;inset:0;background:var(--black);display:flex;flex-direction:column;align-items:center;justify-content:center;z-index:999;gap:20px;transition:opacity 0.6s ease;}
.logo-circle{width:120px;height:120px;border-radius:50%;background:var(--black);border:2px solid var(--gold);display:flex;align-items:center;justify-content:center;box-shadow:0 0 40px rgba(201,168,76,0.3);animation:pulse 2s ease-in-out infinite;}
@keyframes pulse{0%,100%{box-shadow:0 0 30px rgba(201,168,76,0.2)}50%{box-shadow:0 0 60px rgba(201,168,76,0.5)}}
.logo-initials{font-family:'Cormorant Garamond',serif;font-size:42px;font-style:italic;color:var(--gold);line-height:1;}
.brand-name{font-family:'Cormorant Garamond',serif;font-size:22px;letter-spacing:3px;text-transform:uppercase;color:var(--text);}
.brand-sub{font-size:11px;letter-spacing:4px;text-transform:uppercase;color:var(--gold-dim);}
.loading-bar{width:120px;height:1px;background:var(--border);position:relative;overflow:hidden;}
.loading-bar::after{content:'';position:absolute;left:-100%;top:0;height:100%;width:100%;background:linear-gradient(90deg,transparent,var(--gold),transparent);animation:load 1.5s ease forwards 0.3s;}
@keyframes load{to{left:100%}}

/* LAYOUT */
#app{height:100vh;display:none;flex-direction:column;}
.screen{flex:1;overflow-y:auto;overflow-x:hidden;padding-bottom:calc(72px + var(--safe-bottom));display:none;}
.screen.active{display:block;}
.screen::-webkit-scrollbar{display:none;}

/* TOPBAR */
.topbar{background:var(--surface);border-bottom:1px solid var(--border);padding:14px 18px 12px;display:flex;align-items:center;justify-content:space-between;flex-shrink:0;}
.topbar-brand{display:flex;align-items:center;gap:10px;}
.topbar-logo{width:34px;height:34px;border-radius:50%;background:var(--black);border:1px solid var(--gold);display:flex;align-items:center;justify-content:center;font-family:'Cormorant Garamond',serif;font-style:italic;font-size:16px;color:var(--gold);}
.topbar-title{font-family:'Cormorant Garamond',serif;font-size:17px;color:var(--text);letter-spacing:0.5px;}
.topbar-sub{font-size:10px;color:var(--gold-dim);letter-spacing:1px;text-transform:uppercase;}
.topbar-date{font-size:11px;color:var(--text-muted);text-align:right;}
.topbar-datenum{font-family:'Cormorant Garamond',serif;font-size:22px;color:var(--gold);display:block;line-height:1;}

/* BOTTOM NAV */
.bottomnav{position:fixed;bottom:0;left:0;right:0;background:var(--surface);border-top:1px solid var(--border);display:flex;padding-bottom:var(--safe-bottom);z-index:100;}
.nav-btn{flex:1;display:flex;flex-direction:column;align-items:center;gap:3px;padding:10px 0 8px;cursor:pointer;color:var(--text-muted);font-size:10px;letter-spacing:0.5px;text-transform:uppercase;transition:color 0.2s;border:none;background:none;}
.nav-btn.active{color:var(--gold);}
.nav-btn svg{width:22px;height:22px;}
.nav-btn.active svg path,.nav-btn.active svg rect,.nav-btn.active svg circle{stroke:var(--gold);}

/* HOME */
.home-hero{padding:20px 18px 12px;}
.greeting{font-size:13px;color:var(--text-muted);margin-bottom:2px;}
.greeting strong{color:var(--gold);font-family:'Cormorant Garamond',serif;font-size:26px;font-weight:400;display:block;line-height:1.1;}
.stats-row{display:grid;grid-template-columns:1fr 1fr 1fr;gap:10px;padding:0 18px 16px;}
.stat-card{background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius);padding:14px 12px;text-align:center;}
.stat-num{font-family:'Cormorant Garamond',serif;font-size:32px;color:var(--gold);line-height:1;}
.stat-label{font-size:10px;color:var(--text-muted);text-transform:uppercase;letter-spacing:0.5px;margin-top:2px;}
.section-title{padding:0 18px 10px;font-size:11px;text-transform:uppercase;letter-spacing:2px;color:var(--gold-dim);}

/* NOTIF BANNER */
.notif-banner{margin:0 18px 14px;background:rgba(201,168,76,0.08);border:1px solid rgba(201,168,76,0.25);border-radius:12px;padding:12px 14px;display:flex;align-items:center;gap:10px;cursor:pointer;}
.notif-banner svg{flex-shrink:0;}
.notif-banner-text{font-size:12px;color:var(--text-muted);line-height:1.4;}
.notif-banner-text b{color:var(--gold);display:block;font-size:13px;margin-bottom:2px;}
.notif-dot{width:8px;height:8px;border-radius:50%;background:var(--gold);flex-shrink:0;margin-left:auto;animation:pulse 1.5s infinite;}

/* APPOINTMENT CARDS */
.appt-list{padding:0 18px;display:flex;flex-direction:column;gap:10px;}
.appt-card{background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius);padding:14px;display:flex;align-items:center;gap:14px;cursor:pointer;transition:border-color 0.2s;}
.appt-card:active{border-color:var(--gold);}
.appt-time{text-align:center;min-width:44px;}
.appt-hour{font-family:'Cormorant Garamond',serif;font-size:20px;color:var(--gold);line-height:1;}
.appt-ampm{font-size:10px;color:var(--text-muted);}
.appt-divider{width:1px;height:40px;background:var(--border);}
.appt-info{flex:1;}
.appt-client{font-size:15px;font-weight:500;color:var(--text);}
.appt-service{font-size:12px;color:var(--text-muted);margin-top:2px;}
.appt-badge{font-size:10px;padding:3px 8px;border-radius:20px;letter-spacing:0.5px;white-space:nowrap;}
.badge-confirmed{background:rgba(201,168,76,0.15);color:var(--gold);border:1px solid rgba(201,168,76,0.3);}
.badge-pending{background:rgba(255,180,0,0.1);color:#FFB400;border:1px solid rgba(255,180,0,0.2);}
.badge-done{background:rgba(100,200,120,0.1);color:#64C878;border:1px solid rgba(100,200,120,0.2);}
.appt-empty{text-align:center;padding:40px 18px;color:var(--text-muted);font-size:14px;}

/* FAB */
.fab{position:fixed;right:18px;bottom:calc(78px + var(--safe-bottom));width:52px;height:52px;border-radius:50%;background:linear-gradient(135deg,var(--gold),var(--gold-dim));border:none;cursor:pointer;display:flex;align-items:center;justify-content:center;box-shadow:0 4px 20px rgba(201,168,76,0.4);z-index:90;transition:transform 0.2s;}
.fab:active{transform:scale(0.94);}

/* CALENDAR */
.cal-header{padding:16px 18px 0;}
.cal-month{font-family:'Cormorant Garamond',serif;font-size:26px;color:var(--text);display:flex;align-items:center;justify-content:space-between;}
.cal-nav{background:none;border:1px solid var(--border);color:var(--text-muted);width:32px;height:32px;border-radius:8px;cursor:pointer;font-size:16px;display:flex;align-items:center;justify-content:center;}
.cal-grid{padding:10px 12px;}
.cal-days-header{display:grid;grid-template-columns:repeat(7,1fr);text-align:center;margin-bottom:6px;}
.cal-days-header span{font-size:10px;color:var(--text-muted);text-transform:uppercase;letter-spacing:0.5px;}
.cal-days{display:grid;grid-template-columns:repeat(7,1fr);gap:3px;}
.cal-day{min-height:52px;display:flex;flex-direction:column;align-items:center;justify-content:flex-start;padding:5px 3px 4px;border-radius:8px;cursor:pointer;position:relative;font-size:13px;color:var(--text-muted);border:1px solid transparent;transition:border-color 0.15s;}
.cal-day .day-num{line-height:1;font-size:13px;}
.cal-day.has-appt{background:rgba(201,168,76,0.07);border-color:rgba(201,168,76,0.2);}
.cal-day.today .day-num{color:var(--gold);font-weight:700;}
.cal-day.selected{background:rgba(201,168,76,0.18);border-color:var(--gold);}
.cal-day.selected .day-num{color:var(--gold);font-weight:700;}
.cal-day.other-month{opacity:0.2;}
.cal-day:active{background:var(--surface3);}
.cal-name{font-size:8px;color:var(--gold);line-height:1.2;text-align:center;margin-top:3px;overflow:hidden;width:100%;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;word-break:break-word;}
.cal-more{font-size:7px;color:var(--gold-dim);text-align:center;margin-top:1px;}
.day-schedule{padding:0 18px;}
.time-slot{display:flex;gap:14px;margin-bottom:2px;}
.time-label{font-size:11px;color:var(--text-muted);min-width:42px;padding-top:12px;text-align:right;}
.slot-line{flex:1;border-left:1px solid var(--border);padding:8px 0 8px 14px;}
.slot-appt{background:linear-gradient(135deg,rgba(201,168,76,0.15),rgba(201,168,76,0.05));border:1px solid rgba(201,168,76,0.25);border-radius:10px;padding:10px 12px;display:flex;justify-content:space-between;align-items:center;}
.slot-appt .client-name{font-size:14px;font-weight:500;}
.slot-appt .service-tag{font-size:11px;color:var(--gold);margin-top:2px;}
.slot-empty{height:32px;}
.slot-del{background:none;border:none;color:var(--text-muted);font-size:16px;cursor:pointer;padding:4px;}

/* CLIENTS */
.search-bar{padding:14px 18px;position:sticky;top:0;background:var(--black);z-index:10;}
.search-wrap{position:relative;}
.search-input{width:100%;background:var(--surface2);border:1px solid var(--border);border-radius:12px;padding:11px 16px 11px 40px;color:var(--text);font-size:15px;font-family:'Inter',sans-serif;outline:none;}
.search-input:focus{border-color:var(--gold);}
.search-icon{position:absolute;left:14px;top:50%;transform:translateY(-50%);color:var(--text-muted);}
.client-list{padding:0 18px;display:flex;flex-direction:column;gap:8px;}
.client-card{background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius);padding:14px;display:flex;align-items:center;gap:14px;cursor:pointer;}
.client-card:active{border-color:var(--gold);}
.client-avatar{width:44px;height:44px;border-radius:50%;background:linear-gradient(135deg,var(--gold-dim),var(--gold));display:flex;align-items:center;justify-content:center;font-family:'Cormorant Garamond',serif;font-size:19px;color:var(--black);font-weight:600;flex-shrink:0;}
.client-name-text{font-size:15px;font-weight:500;}
.client-last{font-size:12px;color:var(--text-muted);margin-top:2px;}
.client-visits{margin-left:auto;text-align:center;}
.client-visits .num{font-family:'Cormorant Garamond',serif;font-size:22px;color:var(--gold);line-height:1;}
.client-visits .lbl{font-size:10px;color:var(--text-muted);}

/* MODALS */
.modal-overlay{position:fixed;inset:0;background:rgba(0,0,0,0.85);z-index:200;display:flex;flex-direction:column;justify-content:flex-end;opacity:0;pointer-events:none;transition:opacity 0.3s;}
.modal-overlay.open{opacity:1;pointer-events:all;}
.modal-sheet{background:var(--surface);border-top:1px solid var(--border);border-radius:20px 20px 0 0;padding:20px 20px calc(20px + var(--safe-bottom));transform:translateY(100%);transition:transform 0.35s cubic-bezier(0.32,0.72,0,1);max-height:90vh;overflow-y:auto;}
.modal-overlay.open .modal-sheet{transform:translateY(0);}
.modal-handle{width:36px;height:4px;background:var(--border);border-radius:2px;margin:0 auto 20px;}
.modal-title{font-family:'Cormorant Garamond',serif;font-size:22px;margin-bottom:20px;}
.form-group{margin-bottom:14px;}
.form-label{font-size:11px;text-transform:uppercase;letter-spacing:1px;color:var(--gold-dim);margin-bottom:6px;display:block;}
.form-input{width:100%;background:var(--surface2);border:1px solid var(--border);border-radius:10px;padding:12px 14px;color:var(--text);font-size:15px;font-family:'Inter',sans-serif;outline:none;color-scheme:dark;}
.form-input:focus{border-color:var(--gold);}
.service-chips{display:flex;flex-wrap:wrap;gap:8px;}
.chip{padding:7px 14px;border-radius:20px;font-size:13px;border:1px solid var(--border);color:var(--text-muted);cursor:pointer;transition:all 0.2s;}
.chip.selected{border-color:var(--gold);color:var(--gold);background:rgba(201,168,76,0.1);}
.btn-primary{width:100%;padding:15px;background:linear-gradient(135deg,var(--gold),var(--gold-dim));border:none;border-radius:12px;font-size:15px;font-weight:600;color:var(--black);cursor:pointer;margin-top:8px;letter-spacing:0.5px;font-family:'Inter',sans-serif;}
.btn-primary:active{opacity:0.85;}
.btn-danger{width:100%;padding:13px;background:rgba(220,60,60,0.1);border:1px solid rgba(220,60,60,0.25);border-radius:12px;font-size:14px;color:#E05555;cursor:pointer;margin-top:8px;font-family:'Inter',sans-serif;}

/* CLIENT DETAIL */
.detail-header{background:linear-gradient(135deg,var(--surface2),var(--black));border-bottom:1px solid var(--border);padding:20px;text-align:center;}
.detail-avatar{width:70px;height:70px;border-radius:50%;background:linear-gradient(135deg,var(--gold-dim),var(--gold));display:flex;align-items:center;justify-content:center;font-family:'Cormorant Garamond',serif;font-size:30px;color:var(--black);font-weight:600;margin:0 auto 12px;border:2px solid var(--gold);}
.detail-name{font-family:'Cormorant Garamond',serif;font-size:24px;}
.detail-phone{font-size:13px;color:var(--text-muted);margin-top:4px;}
.detail-stats{display:grid;grid-template-columns:1fr 1fr 1fr;padding:16px 20px;border-bottom:1px solid var(--border);}
.detail-stat{text-align:center;}
.detail-stat .n{font-family:'Cormorant Garamond',serif;font-size:26px;color:var(--gold);}
.detail-stat .l{font-size:10px;color:var(--text-muted);text-transform:uppercase;letter-spacing:0.5px;}
.history-list{padding:16px 20px;}
.history-item{display:flex;gap:12px;margin-bottom:14px;}
.history-dot{width:8px;height:8px;border-radius:50%;background:var(--gold);margin-top:5px;flex-shrink:0;}
.history-text{flex:1;}
.history-service{font-size:14px;font-weight:500;}
.history-date{font-size:12px;color:var(--text-muted);margin-top:2px;}
.history-price{font-size:13px;color:var(--gold);margin-top:2px;}
.modal-close{position:absolute;top:16px;right:16px;background:var(--surface3);border:none;color:var(--text-muted);width:32px;height:32px;border-radius:50%;cursor:pointer;font-size:18px;display:flex;align-items:center;justify-content:center;}
.whatsapp-btn{display:flex;align-items:center;justify-content:center;gap:8px;width:100%;padding:13px;background:rgba(37,211,102,0.12);border:1px solid rgba(37,211,102,0.25);border-radius:12px;color:#25D366;font-size:14px;font-weight:500;cursor:pointer;margin:12px 0;}

/* TOAST */
.toast{position:fixed;top:20px;left:50%;transform:translateX(-50%) translateY(-80px);background:var(--surface2);border:1px solid var(--gold);border-radius:12px;padding:12px 20px;font-size:14px;color:var(--text);z-index:999;transition:transform 0.4s cubic-bezier(0.32,0.72,0,1);white-space:nowrap;pointer-events:none;}
.toast.show{transform:translateX(-50%) translateY(0);}

/* NOTIF PERMISSION SCREEN */
.perm-card{margin:20px 18px;background:var(--surface2);border:1px solid rgba(201,168,76,0.3);border-radius:16px;padding:24px;text-align:center;}
.perm-icon{font-size:40px;margin-bottom:12px;}
.perm-title{font-family:'Cormorant Garamond',serif;font-size:22px;color:var(--gold);margin-bottom:8px;}
.perm-desc{font-size:13px;color:var(--text-muted);line-height:1.6;margin-bottom:20px;}
.perm-status{font-size:12px;padding:8px 16px;border-radius:20px;display:inline-block;margin-top:8px;}
.perm-granted{background:rgba(100,200,120,0.1);color:#64C878;border:1px solid rgba(100,200,120,0.2);}
.perm-denied{background:rgba(220,60,60,0.1);color:#E05555;border:1px solid rgba(220,60,60,0.2);}
.perm-pending{background:rgba(201,168,76,0.1);color:var(--gold);border:1px solid rgba(201,168,76,0.2);}

.upcoming-list{padding:0 18px;display:flex;flex-direction:column;gap:8px;margin-top:4px;}
.upcoming-item{background:var(--surface2);border:1px solid var(--border);border-radius:12px;padding:12px 14px;display:flex;align-items:center;gap:12px;}
.upcoming-time{font-size:11px;color:var(--text-muted);text-align:center;min-width:36px;}
.upcoming-time b{font-family:'Cormorant Garamond',serif;font-size:16px;color:var(--gold);display:block;line-height:1;}
.upcoming-info{flex:1;}
.upcoming-client{font-size:13px;font-weight:500;}
.upcoming-service{font-size:11px;color:var(--text-muted);margin-top:1px;}
.upcoming-bell{font-size:14px;}
</style>
</head>
<body>

<div id="splash">
  <div class="logo-circle"><span class="logo-initials">IM</span></div>
  <div style="text-align:center;">
    <div class="brand-name">Irianis Marcano</div>
    <div class="brand-sub">Estilista & Nails</div>
  </div>
  <div class="loading-bar"></div>
</div>

<div id="app">
  <div class="topbar">
    <div class="topbar-brand">
      <div class="topbar-logo">IM</div>
      <div>
        <div class="topbar-title">Irianis Marcano</div>
        <div class="topbar-sub">Estilista & Nails</div>
      </div>
    </div>
    <div class="topbar-date" id="topDate"></div>
  </div>

  <!-- HOME -->
  <div class="screen active" id="screen-home">
    <div class="home-hero">
      <div class="greeting">Bienvenida, <strong>Irianis ✨</strong></div>
    </div>
    <div class="stats-row">
      <div class="stat-card"><div class="stat-num" id="statHoy">0</div><div class="stat-label">Hoy</div></div>
      <div class="stat-card"><div class="stat-num" id="statSemana">0</div><div class="stat-label">Semana</div></div>
      <div class="stat-card"><div class="stat-num" id="statClientas">0</div><div class="stat-label">Clientas</div></div>
    </div>
    <div id="notifBanner"></div>
    <div class="section-title" id="homeDayTitle">Citas de hoy</div>
    <div class="appt-list" id="homeApptList"></div>
    <div style="height:20px;"></div>
  </div>

  <!-- CALENDAR -->
  <div class="screen" id="screen-calendar">
    <div class="cal-header">
      <div class="cal-month">
        <button class="cal-nav" onclick="changeMonth(-1)">‹</button>
        <span id="calMonthLabel"></span>
        <button class="cal-nav" onclick="changeMonth(1)">›</button>
      </div>
    </div>
    <div class="cal-grid">
      <div class="cal-days-header">
        <span>D</span><span>L</span><span>M</span><span>X</span><span>J</span><span>V</span><span>S</span>
      </div>
      <div class="cal-days" id="calDays"></div>
    </div>
    <div class="section-title" id="selectedDayTitle"></div>
    <div class="day-schedule" id="daySchedule"></div>
    <div style="height:20px;"></div>
  </div>

  <!-- CLIENTS -->
  <div class="screen" id="screen-clients">
    <div class="search-bar">
      <div class="search-wrap">
        <svg class="search-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/></svg>
        <input class="search-input" type="text" placeholder="Buscar clienta…" oninput="filterClients(this.value)"/>
      </div>
    </div>
    <div class="client-list" id="clientList"></div>
    <div style="height:20px;"></div>
  </div>

  <!-- NOTIFICACIONES -->
  <div class="screen" id="screen-notif">
    <div class="perm-card">
      <div class="perm-icon">🔔</div>
      <div class="perm-title">Avisos 2 horas antes</div>
      <div class="perm-desc">La app te avisa automáticamente 2 horas antes de cada cita con el nombre de la clienta y el servicio. Funciona aunque tengas el iPhone bloqueado.</div>
      <button class="btn-primary" id="permBtn" onclick="requestNotifPermission()">Activar notificaciones</button>
      <div id="permStatus"></div>
    </div>
    <div class="section-title" style="margin-top:8px;">Próximas citas con aviso</div>
    <div class="upcoming-list" id="upcomingList"></div>
    <div style="height:20px;"></div>
  </div>

  <nav class="bottomnav">
    <button class="nav-btn active" id="nav-home" onclick="goTo('home')">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M3 9.5L12 3l9 6.5V20a1 1 0 01-1 1H4a1 1 0 01-1-1V9.5z"/><path d="M9 21V12h6v9"/></svg>
      Inicio
    </button>
    <button class="nav-btn" id="nav-calendar" onclick="goTo('calendar')">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><rect x="3" y="4" width="18" height="18" rx="2"/><path d="M16 2v4M8 2v4M3 10h18"/></svg>
      Agenda
    </button>
    <button class="nav-btn" id="nav-clients" onclick="goTo('clients')">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><circle cx="12" cy="8" r="4"/><path d="M4 20c0-4 3.6-7 8-7s8 3 8 7"/></svg>
      Clientas
    </button>
    <button class="nav-btn" id="nav-notif" onclick="goTo('notif')">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M18 8A6 6 0 006 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 01-3.46 0"/></svg>
      Avisos
    </button>
  </nav>
</div>

<button class="fab" onclick="openBookingModal()" id="fabBtn">
  <svg viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.5"><path d="M12 5v14M5 12h14"/></svg>
</button>

<!-- BOOKING MODAL -->
<div class="modal-overlay" id="bookingModal" onclick="closeOnOverlay(event,'bookingModal')">
  <div class="modal-sheet">
    <div class="modal-handle"></div>
    <div class="modal-title">Nueva Reserva</div>
    <div class="form-group">
      <label class="form-label">Clienta</label>
      <input class="form-input" id="bName" type="text" placeholder="Nombre completo"/>
    </div>
    <div class="form-group">
      <label class="form-label">Teléfono (opcional)</label>
      <input class="form-input" id="bPhone" type="tel" placeholder="+1 555 0000"/>
    </div>
    <div class="form-group">
      <label class="form-label">Fecha y hora</label>
      <input class="form-input" id="bDatetime" type="datetime-local"/>
    </div>
    <div class="form-group">
      <label class="form-label">Servicio</label>
      <div class="service-chips" id="serviceChips">
        <span class="chip" onclick="toggleChip(this)">Manicure</span>
        <span class="chip" onclick="toggleChip(this)">Pedicure</span>
        <span class="chip" onclick="toggleChip(this)">Gel</span>
        <span class="chip" onclick="toggleChip(this)">Extensiones</span>
        <span class="chip" onclick="toggleChip(this)">Tinte</span>
        <span class="chip" onclick="toggleChip(this)">Corte</span>
        <span class="chip" onclick="toggleChip(this)">Alisado</span>
        <span class="chip" onclick="toggleChip(this)">Peinado</span>
        <span class="chip" onclick="toggleChip(this)">Maquillaje</span>
        <span class="chip" onclick="toggleChip(this)">Cejas</span>
      </div>
    </div>
    <div class="form-group">
      <label class="form-label">Notas</label>
      <input class="form-input" id="bNotes" type="text" placeholder="Ej: alérgica a amoniaco…"/>
    </div>
    <button class="btn-primary" onclick="saveBooking()">Guardar reserva ✓</button>
  </div>
</div>

<!-- CLIENT DETAIL MODAL -->
<div class="modal-overlay" id="clientModal" onclick="closeOnOverlay(event,'clientModal')">
  <div class="modal-sheet" style="position:relative;">
    <button class="modal-close" onclick="closeModal('clientModal')">✕</button>
    <div class="modal-handle"></div>
    <div class="detail-header">
      <div class="detail-avatar" id="detailAvatar">M</div>
      <div class="detail-name" id="detailName"></div>
      <div class="detail-phone" id="detailPhone"></div>
    </div>
    <div class="detail-stats">
      <div class="detail-stat"><div class="n" id="detailVisits">0</div><div class="l">Visitas</div></div>
      <div class="detail-stat"><div class="n" id="detailSince">—</div><div class="l">Desde</div></div>
      <div class="detail-stat"><div class="n" id="detailNext">—</div><div class="l">Próxima</div></div>
    </div>
    <div class="history-list">
      <div class="section-title" style="padding:0 0 10px;">Historial</div>
      <div id="detailHistory"></div>
    </div>
    <div style="padding:0 0 4px;">
      <div class="whatsapp-btn" id="waBtn">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="#25D366"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z"/></svg>
        Enviar recordatorio por WhatsApp
      </div>
    </div>
  </div>
</div>

<div class="toast" id="toast"></div>

<script>
// ═══════════════════════════════════════
// STORAGE
// ═══════════════════════════════════════
function loadBookings() {
  try { return JSON.parse(localStorage.getItem('im_bookings') || '[]'); } catch { return []; }
}
function saveBookings(b) { localStorage.setItem('im_bookings', JSON.stringify(b)); }
function loadClients() {
  try { return JSON.parse(localStorage.getItem('im_clients') || '[]'); } catch { return []; }
}
function saveClients(c) { localStorage.setItem('im_clients', JSON.stringify(c)); }

// ═══════════════════════════════════════
// NOTIFICATIONS — 2 HORAS ANTES
// ═══════════════════════════════════════
let notifTimers = [];

function clearNotifTimers() {
  notifTimers.forEach(t => clearTimeout(t));
  notifTimers = [];
}

function scheduleNotifications() {
  clearNotifTimers();
  if (Notification.permission !== 'granted') return;
  const bookings = loadBookings();
  const now = Date.now();
  bookings.forEach(b => {
    const apptTime = new Date(b.datetime).getTime();
    const alertTime = apptTime - 2 * 60 * 60 * 1000; // 2 horas antes
    const delay = alertTime - now;
    if (delay > 0 && delay < 7 * 24 * 60 * 60 * 1000) { // dentro de los próximos 7 días
      const t = setTimeout(() => {
        const h = new Date(b.datetime).toLocaleTimeString('es', {hour:'2-digit',minute:'2-digit'});
        new Notification('💅 Irianis — Cita en 2 horas', {
          body: `${b.client} · ${b.service} a las ${h}`,
          icon: 'icons/icon-192.png',
          badge: 'icons/icon-192.png',
          tag: 'cita-' + b.id,
        });
      }, delay);
      notifTimers.push(t);
    }
  });
}

async function requestNotifPermission() {
  if (!('Notification' in window)) {
    showToast('⚠️ Este navegador no soporta notificaciones');
    return;
  }
  const result = await Notification.requestPermission();
  updatePermUI();
  if (result === 'granted') {
    scheduleNotifications();
    showToast('🔔 Notificaciones activadas');
  } else {
    showToast('⚠️ Permiso denegado. Actívalo en Ajustes del iPhone');
  }
}

function updatePermUI() {
  const perm = ('Notification' in window) ? Notification.permission : 'unsupported';
  const btn = document.getElementById('permBtn');
  const status = document.getElementById('permStatus');
  const banner = document.getElementById('notifBanner');

  if (perm === 'granted') {
    btn.textContent = '✓ Notificaciones activas';
    btn.style.opacity = '0.6';
    btn.onclick = null;
    status.innerHTML = '<span class="perm-status perm-granted">✓ Activadas — recibirás aviso 2 horas antes de cada cita</span>';
    if (banner) banner.innerHTML = '';
  } else if (perm === 'denied') {
    btn.textContent = 'Permiso bloqueado';
    btn.style.opacity = '0.5';
    status.innerHTML = '<span class="perm-status perm-denied">Bloqueado. Ve a Ajustes del iPhone → Safari → Notificaciones</span>';
  } else {
    status.innerHTML = '<span class="perm-status perm-pending">Pendiente de activar</span>';
    if (banner) banner.innerHTML = `
      <div class="notif-banner" onclick="goTo('notif')">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2"><path d="M18 8A6 6 0 006 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 01-3.46 0"/></svg>
        <div class="notif-banner-text"><b>Activa los avisos</b>Recibe una notificación 2 horas antes de cada cita</div>
        <div class="notif-dot"></div>
      </div>`;
  }
  renderUpcoming();
}

function renderUpcoming() {
  const bookings = loadBookings().sort((a,b) => new Date(a.datetime)-new Date(b.datetime));
  const now = Date.now();
  const future = bookings.filter(b => new Date(b.datetime).getTime() > now).slice(0, 8);
  const el = document.getElementById('upcomingList');
  if (!el) return;
  if (!future.length) { el.innerHTML = '<p style="padding:20px 18px;color:var(--text-muted);font-size:14px;">No hay citas próximas agendadas</p>'; return; }
  el.innerHTML = future.map(b => {
    const d = new Date(b.datetime);
    const alertMs = d.getTime() - 2*60*60*1000 - now;
    const bell = Notification.permission === 'granted' && alertMs > 0 ? '🔔' : '○';
    return `<div class="upcoming-item">
      <div class="upcoming-time"><b>${d.toLocaleTimeString('es',{hour:'2-digit',minute:'2-digit'})}</b>${d.toLocaleDateString('es',{day:'numeric',month:'short'})}</div>
      <div class="upcoming-info"><div class="upcoming-client">${b.client}</div><div class="upcoming-service">${b.service}</div></div>
      <div class="upcoming-bell">${bell}</div>
    </div>`;
  }).join('');
}

// ═══════════════════════════════════════
// BOOKING
// ═══════════════════════════════════════
function openBookingModal() {
  // Default datetime = next hour
  const d = new Date(); d.setHours(d.getHours()+1, 0, 0, 0);
  document.getElementById('bDatetime').value = d.toISOString().slice(0,16);
  document.getElementById('bName').value = '';
  document.getElementById('bPhone').value = '';
  document.getElementById('bNotes').value = '';
  document.querySelectorAll('#serviceChips .chip').forEach(c => c.classList.remove('selected'));
  document.getElementById('bookingModal').classList.add('open');
}

function saveBooking() {
  const name = document.getElementById('bName').value.trim();
  const phone = document.getElementById('bPhone').value.trim();
  const dt = document.getElementById('bDatetime').value;
  const notes = document.getElementById('bNotes').value.trim();
  const services = [...document.querySelectorAll('#serviceChips .chip.selected')].map(c=>c.textContent).join(', ');
  if (!name) { showToast('⚠️ Escribe el nombre de la clienta'); return; }
  if (!dt) { showToast('⚠️ Selecciona fecha y hora'); return; }

  const bookings = loadBookings();
  const booking = { id: Date.now(), client: name, phone, datetime: dt, service: services || 'Servicio general', notes };
  bookings.push(booking);
  saveBookings(bookings);

  // Update clients
  const clients = loadClients();
  let ci = clients.find(c => c.name.toLowerCase() === name.toLowerCase());
  if (!ci) {
    ci = { name, phone, visits: 0, since: new Date().getFullYear().toString(), history: [] };
    clients.push(ci);
  }
  ci.visits++;
  ci.phone = phone || ci.phone;
  const dateStr = new Date(dt).toLocaleDateString('es',{day:'numeric',month:'short',year:'numeric'});
  ci.history.unshift({ service: services || 'Servicio general', date: dateStr, price: '' });
  saveClients(clients);

  closeModal('bookingModal');
  scheduleNotifications();
  refreshAll();
  showToast('🗓 Reserva guardada con éxito');
}

function deleteBooking(id) {
  if (!confirm('¿Eliminar esta reserva?')) return;
  const bookings = loadBookings().filter(b => b.id !== id);
  saveBookings(bookings);
  scheduleNotifications();
  refreshAll();
  showToast('🗑 Reserva eliminada');
}

// ═══════════════════════════════════════
// HOME
// ═══════════════════════════════════════
const MONTHS = ['Enero','Febrero','Marzo','Abril','Mayo','Junio','Julio','Agosto','Septiembre','Octubre','Noviembre','Diciembre'];

function refreshHome() {
  const now = new Date();
  const todayStr = now.toDateString();
  const bookings = loadBookings();
  const todayAppts = bookings.filter(b => new Date(b.datetime).toDateString() === todayStr)
    .sort((a,b) => new Date(a.datetime)-new Date(b.datetime));

  // Week count
  const startOfWeek = new Date(now); startOfWeek.setDate(now.getDate() - now.getDay()); startOfWeek.setHours(0,0,0,0);
  const endOfWeek = new Date(startOfWeek); endOfWeek.setDate(startOfWeek.getDate()+7);
  const weekCount = bookings.filter(b => { const d=new Date(b.datetime); return d>=startOfWeek && d<endOfWeek; }).length;

  document.getElementById('statHoy').textContent = todayAppts.length;
  document.getElementById('statSemana').textContent = weekCount;
  document.getElementById('statClientas').textContent = loadClients().length;

  const days = ['Domingo','Lunes','Martes','Miércoles','Jueves','Viernes','Sábado'];
  document.getElementById('homeDayTitle').textContent = `${days[now.getDay()]} ${now.getDate()} — Citas de hoy`;

  const list = document.getElementById('homeApptList');
  if (!todayAppts.length) {
    list.innerHTML = '<div class="appt-empty">Sin citas agendadas para hoy 🌸</div>';
    return;
  }
  list.innerHTML = todayAppts.map(b => {
    const d = new Date(b.datetime);
    const h = d.getHours(), m = d.getMinutes();
    const hStr = (h>12?h-12:h||12)+'';
    const mStr = m.toString().padStart(2,'0');
    const ampm = h>=12?'PM':'AM';
    const nowMs = Date.now(), apptMs = d.getTime();
    const badge = apptMs < nowMs ? 'badge-done">Lista' : 'badge-confirmed">Confirmada';
    return `<div class="appt-card">
      <div class="appt-time"><div class="appt-hour">${hStr}:${mStr}</div><div class="appt-ampm">${ampm}</div></div>
      <div class="appt-divider"></div>
      <div class="appt-info"><div class="appt-client">${b.client}</div><div class="appt-service">${b.service}</div></div>
      <span class="appt-badge ${badge}</span>
    </div>`;
  }).join('');
}

// ═══════════════════════════════════════
// CALENDAR
// ═══════════════════════════════════════
let calYear = new Date().getFullYear(), calMonth = new Date().getMonth();
let selectedDay = new Date().getDate();

function renderCalendar() {
  document.getElementById('calMonthLabel').textContent = `${MONTHS[calMonth]} ${calYear}`;
  const first = new Date(calYear, calMonth, 1).getDay();
  const days = new Date(calYear, calMonth+1, 0).getDate();
  const prevDays = new Date(calYear, calMonth, 0).getDate();
  const bookings = loadBookings();
  const today = new Date();

  // Build day→clients map
  const dayMap = {};
  bookings.forEach(b => {
    const d = new Date(b.datetime);
    if (d.getFullYear()===calYear && d.getMonth()===calMonth) {
      const day = d.getDate();
      if (!dayMap[day]) dayMap[day] = [];
      dayMap[day].push(b.client.split(' ')[0]);
    }
  });

  let html = '';
  for(let i=0;i<first;i++) html += `<div class="cal-day other-month"><span class="day-num">${prevDays-first+1+i}</span></div>`;
  for(let d=1;d<=days;d++){
    const isToday = calYear===today.getFullYear() && calMonth===today.getMonth() && d===today.getDate();
    const isSel = d===selectedDay;
    const names = dayMap[d] || [];
    const shown = names.slice(0,2);
    const extra = names.length>2 ? names.length-2 : 0;
    html += `<div class="cal-day${isToday?' today':''}${isSel?' selected':''}${names.length?' has-appt':''}" onclick="selectDay(this,${d})">
      <span class="day-num">${d}</span>
      ${shown.map(n=>`<div class="cal-name">${n}</div>`).join('')}
      ${extra?`<div class="cal-more">+${extra}</div>`:''}
    </div>`;
  }
  document.getElementById('calDays').innerHTML = html;
  renderDaySchedule(selectedDay);
}

function renderDaySchedule(d) {
  const bookings = loadBookings().filter(b => {
    const bd = new Date(b.datetime);
    return bd.getFullYear()===calYear && bd.getMonth()===calMonth && bd.getDate()===d;
  }).sort((a,b)=>new Date(a.datetime)-new Date(b.datetime));

  document.getElementById('selectedDayTitle').textContent = `${d} de ${MONTHS[calMonth]} — ${bookings.length} cita${bookings.length!==1?'s':''}`;

  if (!bookings.length) {
    document.getElementById('daySchedule').innerHTML = '<div class="appt-empty">Sin citas este día</div>';
    return;
  }
  document.getElementById('daySchedule').innerHTML = bookings.map(b => {
    const dt = new Date(b.datetime);
    const timeStr = dt.toLocaleTimeString('es',{hour:'2-digit',minute:'2-digit'});
    return `<div class="time-slot">
      <div class="time-label">${timeStr}</div>
      <div class="slot-line">
        <div class="slot-appt">
          <div><div class="client-name">${b.client}</div><div class="service-tag">${b.service}</div>${b.notes?`<div style="font-size:11px;color:var(--text-muted);margin-top:2px;">📝 ${b.notes}</div>`:''}</div>
          <button class="slot-del" onclick="deleteBooking(${b.id})" title="Eliminar">✕</button>
        </div>
      </div>
    </div>`;
  }).join('');
}

function selectDay(el, d) {
  selectedDay = d;
  renderCalendar();
}
function changeMonth(dir) {
  calMonth+=dir;
  if(calMonth>11){calMonth=0;calYear++;}
  if(calMonth<0){calMonth=11;calYear--;}
  selectedDay = 1;
  renderCalendar();
}

// ═══════════════════════════════════════
// CLIENTS
// ═══════════════════════════════════════
function renderClients(list) {
  const el = document.getElementById('clientList');
  if (!list.length) { el.innerHTML = '<p style="padding:20px 0;color:var(--text-muted);text-align:center;font-size:14px;">Sin clientas registradas aún</p>'; return; }
  el.innerHTML = list.map((c,i) => `
    <div class="client-card" onclick="openClient(${i})">
      <div class="client-avatar">${c.name[0].toUpperCase()}</div>
      <div style="flex:1;overflow:hidden;">
        <div class="client-name-text">${c.name}</div>
        <div class="client-last">${c.history[0]?.date||'—'} · ${c.history[0]?.service||''}</div>
      </div>
      <div class="client-visits"><div class="num">${c.visits}</div><div class="lbl">visitas</div></div>
    </div>`).join('');
}

function filterClients(q) {
  const clients = loadClients();
  renderClients(clients.filter(c => c.name.toLowerCase().includes(q.toLowerCase())));
}

let currentClientIdx = -1;
function openClient(i) {
  const clients = loadClients();
  const c = clients[i]; currentClientIdx = i;
  document.getElementById('detailAvatar').textContent = c.name[0].toUpperCase();
  document.getElementById('detailName').textContent = c.name;
  document.getElementById('detailPhone').textContent = c.phone || 'Sin teléfono';
  document.getElementById('detailVisits').textContent = c.visits;
  document.getElementById('detailSince').textContent = c.since;
  // next appointment
  const future = loadBookings().filter(b => b.client.toLowerCase()===c.name.toLowerCase() && new Date(b.datetime)>new Date())
    .sort((a,b)=>new Date(a.datetime)-new Date(b.datetime));
  const nextAppt = future[0] ? new Date(future[0].datetime).toLocaleDateString('es',{day:'numeric',month:'short'}) : '—';
  document.getElementById('detailNext').textContent = nextAppt;
  document.getElementById('detailHistory').innerHTML = c.history.length
    ? c.history.map(h=>`<div class="history-item"><div class="history-dot"></div><div class="history-text"><div class="history-service">${h.service}</div><div class="history-date">${h.date}</div>${h.price?`<div class="history-price">${h.price}</div>`:''}</div></div>`).join('')
    : '<p style="color:var(--text-muted);font-size:13px;">Sin historial aún</p>';

  const waBtn = document.getElementById('waBtn');
  if (c.phone) {
    const msg = encodeURIComponent(`Hola ${c.name.split(' ')[0]}! 💅 Te recuerdo tu cita con Irianis Marcano ${nextAppt !== '—' ? 'el ' + nextAppt : 'próximamente'}. ¡Te esperamos!`);
    waBtn.onclick = () => window.open(`https://wa.me/${c.phone.replace(/\D/g,'')}?text=${msg}`);
    waBtn.style.opacity = '1';
  } else {
    waBtn.onclick = () => showToast('⚠️ Agrega el teléfono de la clienta');
    waBtn.style.opacity = '0.5';
  }
  document.getElementById('clientModal').classList.add('open');
}

// ═══════════════════════════════════════
// HELPERS
// ═══════════════════════════════════════
function closeModal(id) { document.getElementById(id).classList.remove('open'); }
function closeOnOverlay(e, id) { if(e.target===document.getElementById(id)) closeModal(id); }
function toggleChip(el) { el.classList.toggle('selected'); }
function goTo(screen) {
  document.querySelectorAll('.screen').forEach(s=>s.classList.remove('active'));
  document.querySelectorAll('.nav-btn').forEach(b=>b.classList.remove('active'));
  document.getElementById('screen-'+screen).classList.add('active');
  document.getElementById('nav-'+screen).classList.add('active');
  if(screen==='calendar') renderCalendar();
  if(screen==='clients') renderClients(loadClients());
  if(screen==='notif') { updatePermUI(); renderUpcoming(); }
}
function showToast(msg) {
  const t = document.getElementById('toast'); t.textContent = msg; t.classList.add('show');
  setTimeout(()=>t.classList.remove('show'), 3000);
}
function refreshAll() { refreshHome(); renderCalendar(); renderClients(loadClients()); }

// ═══════════════════════════════════════
// INIT
// ═══════════════════════════════════════
// Service Worker
if ('serviceWorker' in navigator) navigator.serviceWorker.register('sw.js').catch(()=>{});

// Date header
const now2 = new Date();
document.getElementById('topDate').innerHTML = `<span class="topbar-datenum">${now2.getDate()}</span>${MONTHS[now2.getMonth()].slice(0,3)} ${now2.getFullYear()}`;

// Splash
setTimeout(() => {
  document.getElementById('splash').style.opacity = '0';
  setTimeout(() => {
    document.getElementById('splash').style.display = 'none';
    document.getElementById('app').style.display = 'flex';
    refreshAll();
    updatePermUI();
    scheduleNotifications();
  }, 600);
}, 1800);
</script>
</body>
</html>
HTMLEOF
echo "Done"
